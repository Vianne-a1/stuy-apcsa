/* Have: 2 input file
Driver:
Main Class:
    - Main method (return final value/output of code)
    - Helper methods:
        - Array creation based on input files
        - Getting the values of the byte vertically and horizontally next to the byte (loop through this until all are done, if tree (*) is in the way, ignore)
        - Add all those values and set it to the current byte, clear the bytes used to caluclate other bytes.


        Bytes** = "boxes"/array values

        */