public int findFreeBlock(int period, int duration){
    int p = period;
    int time = duration;
    if (AppointmentBook(p, duration){
for(int i=0, i<60; i++){
    if isMinuteFree(2, time){
        
    }
}
        return -1
    }
}